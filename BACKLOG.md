# TravelPlanner — Product Backlog

**Version:** 1.0
**Date:** 2026-04-24
**Author:** [product-manager]
**Status:** Active
**Changelog:** v1.0 — Initial backlog creation (Sprint 0) · v1.1 — B-001..B-006, B-018 moved to in-progress for Sprint 1 · v1.2 — B-001..B-006, B-018 done after Sprint 1 close · v1.3 — B-019 added (invitation-only access) for Sprint 2 · v1.4 — B-009/B-010/B-011/B-012/B-019 done after Sprint 2 close · v1.5 — B-007/B-008/B-013 moved to in-progress for Sprint 3; AC additions per R1 · v1.6 — B-007/B-008/B-013 done after Sprint 3 close (2026-04-28); ownership-transfer remains a future backlog candidate · v1.7 — B-016 AC fully refined in R1 Sprint 4 (dry-run, idempotency key, place-resolution, error-handling, non-dated list handling) · v1.7 — B-014/B-016/B-017 moved to in-progress for Sprint 4; B-014 AC fully refined per R1 (2026-04-28)

---

## Backlog Legend

**Priority**
- **P0**: Critical — blocks launch or core value
- **P1**: High — essential for competitive product
- **P2**: Medium — important but not blocking
- **P3**: Nice-to-have — improves experience

**Effort**
- **XS**: < 2 hours
- **S**: 2–8 hours
- **M**: 1–3 days
- **L**: 3–5 days
- **XL**: 5–10 days

**Status**
- **done**: Completed and verified
- **in-progress**: Currently being worked on
- **backlog**: Not started

---

## Phase A — Foundation

| ID | User Story | Priority | Effort | Status | Sprint | Dependencies | Acceptance Criteria |
|----|-----------|----------|--------|--------|--------|--------------|---------------------|
| B-001 | **Auth — Sign up**: As a new user I want to create an account with my email and password so that I have a secure personal workspace for my trips. | P0 | M | done | 1 | — | (1) Sign-up form validates: email format, password ≥ 12 chars, passwords match. (2) Duplicate email returns a clear "Email already registered" message without leaking whether the account exists. (3) Supabase Auth user created; `profiles` row created on confirmation. (4) Confirmation email sent (Supabase default). (5) Unconfirmed users cannot access protected routes. (6) Rate limit: 5 sign-up attempts per IP per 15 minutes. |
| B-002 | **Auth — Sign in**: As a registered user I want to sign in with my email and password so that I can access my trips from any device. | P0 | M | done | 1 | B-001 | (1) Sign-in form validates: email and password fields required. (2) Invalid credentials return a generic "Email or password incorrect" message (no account enumeration). (3) Successful sign-in creates a Supabase session; user redirected to `/trips`. (4) Session persisted in a secure, httpOnly cookie. (5) Rate limit: 5 failed attempts per IP per 15 minutes triggers a 15-minute lockout. |
| B-003 | **Auth — Sign out and password reset**: As a signed-in user I want to sign out and, if I forget my password, request a reset email so that I remain in control of my account security. | P0 | S | done | 1 | B-002 | (1) Sign-out clears the session cookie and redirects to `/`. (2) "Forgot password" link on sign-in page submits email and triggers Supabase password-reset email. (3) Reset link expires after 1 hour. (4) After resetting, old session tokens are invalidated. (5) Sign-out and reset endpoints require no additional auth beyond the current session. |
| B-004 | **Trips CRUD**: As a trip organizer I want to create, view, edit, and delete my trips so that I have a structured record for each journey. | P0 | M | done | 1 | B-001 | (1) Create form requires: trip name, start date, end date, base currency; destination and budget are optional. (2) On create, `trips` row saved with `owner_id = auth.uid()`; `trip_members` row with role `owner` created; `trip_days` rows generated for each calendar day inclusive. (3) Trip list shows only trips where user is a member; sorted by start date desc; paginated at 20 per page. (4) Edit form pre-filled with existing values; extending dates generates new `trip_days`; shrinking dates that would remove days containing items is blocked with a warning. (5) Delete requires typing the trip name in a confirmation dialog; cascade deletes all child rows. |
| B-005 | **Trip days — day view**: As a trip organizer I want to see and navigate a day-by-day structure for my trip so that I can plan each day's activities in sequence. | P0 | M | done | 1 | B-004 | (1) `/trips/[id]/itinerary` lists all `trip_days` rows in date order. (2) Each day shows: date (formatted via `Intl.DateTimeFormat`), day number (Day 1, Day 2…), optional day title (editable inline by editor/owner), count of items. (3) Day title edit saves on blur or Enter. (4) Empty days show an "Add first item" CTA. (5) Navigation between days via scroll or a day-picker sidebar. (6) Viewer role can read day titles but cannot edit them. |
| B-006 | **Itinerary items CRUD**: As a trip organizer I want to add, edit, and delete typed itinerary items (transport, lodging, activity, meal, note) on each trip day so that the full plan is captured in one place. | P0 | M | done | 1 | B-005 | (1) "Add item" button on each day opens a type-selection step. (2) Common fields for all types: title (required, max 200 chars), start time, end time, notes, cost, currency. (3) Items sorted by start time within the day (null times appended last). (4) Edit opens a pre-filled form. (5) Delete requires a confirmation modal with the item title. (6) Only editor/owner can add, edit, or delete items. (7) `trip_id` and `day_id` are set server-side from the URL context — never accepted from the request body. |
| B-007 | **Transportation fields**: As a trip organizer I want to record structured details for flights, trains, and other transport segments (carrier, confirmation, times, from/to) so that I have all booking info in one place. | P1 | M | done | 3 | B-006 | (1) When itinerary item type is "transport," extra fields appear: mode (flight/train/bus/car/ferry), carrier/operator, confirmation number, departure location, arrival location, departure datetime, arrival datetime. (2) A `transportation` row is created linked to the `itinerary_items` row via `itinerary_item_id` FK (1:1, ON DELETE CASCADE). (3) Departure datetime stored in UTC; displayed in browser locale format. (4) Confirmation number displayed as plain text. (5) Transport summary section on trip overview lists all segments in departure-time order. (6) `GET /api/trips/[id]/transportation` responds in < 500ms P95; no N+1; uses `departure_time` index for ordering; paginated 20/page. (7) Creating a transport itinerary item is wrapped in a single DB transaction — either both `itinerary_items` and `transportation` inserts succeed, or both roll back. (8) Editing a `type=transport` item updates both rows in one transaction. Changing `type` away from `transport` deletes the linked `transportation` row. (9) Form accepts local-time inputs in the user's browser time zone; API receives ISO-8601 with offset; stored as UTC `timestamptz`. (10) Cost and currency for a transport segment live on `transportation.cost` / `transportation.currency` only; `itinerary_items.cost` is null for transport-type items. |
| B-008 | **Accommodations**: As a trip organizer I want to log hotel stays with check-in/check-out dates and confirmation details spanning multiple days so that lodging is visible across the full itinerary. | P1 | M | done | 3 | B-005 | (1) "Add accommodation" form: hotel name or place link, check-in date, check-out date, confirmation number, cost per night, total cost, notes. A single currency field applies to both cost fields. (2) Check-in must be within trip date range; check-out must be on or after check-in date (same-day stays allowed); both within trip range. (3) `accommodations` row created (no FK to `itinerary_items`; FK to `trips` only). (4) On the check-in day view, a "Check in: [name]" indicator appears; on check-out day, a "Check out: [name]" indicator. On every intermediate day between check-in and check-out, a "Staying at: [name]" indicator appears. (5) Accommodations listed on trip overview in check-in date order with name, dates, and total cost. (6) `GET /api/trips/[id]/accommodations` responds in < 500ms P95; ordered by `check_in_date` ASC; no N+1. (7) Cost-per-night and total-cost are stored independently — neither is auto-derived from the other; both share the single currency field. (8) Accommodation can be edited (pre-filled form, same validation) and deleted (confirmation modal); deletion removes all check-in/intermediate/check-out indicators from affected day views. (9) Entry points: "Add accommodation" button on the trip's Accommodations tab and on each day view. |
| B-009 | **Google Places search proxy**: As a trip organizer I want to search for places by name and see results with addresses and coordinates so that I can link real places to my itinerary and bookmarks without leaving the app. | P1 | L | done | 2 | B-001 | (1) `/api/places/search?q=[query]` endpoint accepts query string; authenticates caller via session. (2) Google Places API key held server-side only — never returned to client. (3) Results checked against `places` cache table first (match on `google_place_id`); cache hit returns cached data. (4) Cache miss calls Google Places API, stores result in `places` table, returns to client. (5) Rate limit: 30 requests/minute per user. (6) Returns: `google_place_id`, name, address, lat, lng, category. (7) Search fires after 2+ characters; results appear within 800ms for cache hits. |
| B-010 | **Place detail cache and page**: As a trip organizer I want to view a rich place detail page (photos, hours, rating, website) so that I can evaluate a restaurant or sight before bookmarking it. | P1 | M | done | 2 | B-009 | (1) `/api/places/[googlePlaceId]` fetches full place details from Google Places API if not cached or TTL expired (7 days). (2) Full details stored in `places.cached_details` JSONB with `cached_at` timestamp. (3) Place detail page at `/places/[id]` renders from cache: name, photos (up to 5), address, rating (1–5), opening hours, website, Google Maps link. (4) Google attribution text displayed on all place data surfaces. (5) If place is not yet cached (first visit), page shows a loading state while the API call completes. |
| B-011 | **Bookmarks**: As a trip organizer I want to bookmark places (restaurants, sights, museums, shopping) for my trip so that I have a curated shortlist of places I want to visit. | P1 | M | done | 2 | B-010, B-004 | (1) "Bookmark for [trip name]" button on place detail page creates a `bookmarks` row: `trip_id`, `place_id`, `category`, `notes`, `user_id`. (2) Category is pre-filled from the place's Google type but editable (restaurant / sight / museum / shopping). (3) Duplicate bookmark (same trip + place) is rejected with "Already bookmarked" feedback — no duplicate row created. (4) Bookmarks listed on trip Places tab grouped by category, sorted by name within group. (5) Viewer can view bookmarks but cannot add or delete. (6) Editor/owner can add and delete bookmarks. |
| B-012 | **Trip member invite and accept**: As a trip owner I want to invite someone by email to join my trip as an editor or viewer so that we can plan together. | P1 | M | done | 2 | B-001, B-004 | (1) Owner enters email + selects role (editor/viewer) on Members tab; submits. (2) `trip_invitations` row created: `trip_id`, `email`, `role`, `token` (cryptographically random 32-byte hex), `expires_at` = now + 48h. (3) Invitation link displayed on screen: `/invite/[token]` (email delivery deferred to Phase B). (4) Invitee visits link; if not authenticated, redirected to sign-in/sign-up with return URL. (5) On accept: token validated (exists, not expired, not used); `trip_members` row created with the invitation's role; `trip_invitations.used_at` set. (6) Invitee redirected to trip overview. (7) Expired/invalid token shows a clear error; no trip data exposed. |
| B-013 | **Member role management**: As a trip owner I want to view all trip members, change their roles, and remove members so that I control who can edit or view the trip. | P1 | S | done | 3 | B-012 | (1) Members tab lists all accepted members: display name or email, role badge, join date. (2) Owner can change a member's role via dropdown (owner/editor/viewer); change takes effect immediately. Promoting a member to `owner` does NOT demote the existing owner — multiple concurrent owners are allowed and have equal authority. (3) Owner cannot remove themselves; the only recourse is to delete the trip. (Ownership transfer is deferred to a later sprint as a separate backlog item.) (4) Removing a member requires a confirmation modal; on confirm, `trip_members` row deleted. (5) Removed member no longer sees the trip in their list. (6) Viewer can see the member list but cannot change roles or remove members. (7) `GET /api/trips/[id]/members` responds in < 500ms P95; member list joins profile data in a single query (no N+1). (8) Removing a member does NOT delete their authored content. `itinerary_items.created_by`, `bookmarks.added_by`, `expenses.paid_by` are set to null by DB cascade; the content remains visible to remaining members. (9) A removed member's active session receives 403 on subsequent trip API calls; frontend redirects to `/trips` with toast "You no longer have access to this trip." (10) Pending (unaccepted) invitations are NOT shown on the Members tab — they are managed separately (out of B-013 scope). (11) `PATCH /api/trips/[id]/members/[userId]` (role change) and `DELETE /api/trips/[id]/members/[userId]` (remove) are added; both gated to owner-only at API + RLS layer. |
| B-014 | **Budget and expenses**: As a trip organizer I want to set a trip budget and log categorized expenses with amount, currency, who paid, and who it is split among so that I can track spending against my budget. | P1 | M | done | 4 | B-004, B-012 | (1) Trip budget (optional) set on trip create or edit; stored in `trips.total_budget` in `base_currency`. (2) "Add expense" form: category (accommodation/transport/food/activities/shopping/other), description, amount, currency (v1: only `base_currency` accepted — FX multi-currency deferred to Phase B), date (required; must fall within trip's start_date..end_date), paid_by (trip member dropdown, defaults to current user), split_among (multi-select of trip members, defaults to all accepted members). (3) v1 split model is EQUAL: each selected member bears `amount ÷ count(split_among)` — weighted/percentage splits deferred to Phase B. `split_among` stored as `jsonb` array of `{ user_id, share_pct }` where `share_pct = 100 / count` for each member (equal distribution; values sum to 100). (4) `expenses` row created server-side; `trip_id` set from URL context — never from request body. (5) Expense `date` validated server-side: must be within trip `start_date`..`end_date` inclusive. (6) Trip overview Budget section shows: total budget (if set), total spent (sum of all expense amounts in base_currency), remaining = budget − spent (shown in red when negative; hidden if no budget set). (7) Budget tab lists all expenses in `occurred_at` descending order; filterable by category and paid_by; paginated 20/page. `GET /api/trips/[id]/expenses` responds in < 500ms P95; no N+1. (8) Edit expense: pre-filled form with same validation as create; editor/owner only. (9) Delete expense: requires a confirmation modal showing description + amount; editor/owner only. (10) Per-member balance section on Budget tab (server-computed, never client-computed): for each accepted trip member — `paid = sum(expenses where paid_by = member)`, `owed = sum(expense.amount / count(split_among) for each expense where member is in split_among)`, `net_balance = paid − owed` (positive = is owed money; negative = owes money); displayed as a table. (11) Role rules: editor and owner can create, edit, and delete expenses; viewer has read-only access — "Add expense," edit, and delete controls are hidden. (12) Audit log entry created on every expense insert, update, and delete. |
| B-015 | **Leaflet map — day view**: As a trip member I want to see a map of all the places on a given day so that I can visualize the route and proximity of stops. | P2 | M | backlog | 4 | B-006, B-010 | (1) Each day view includes a collapsible map section rendered with `react-leaflet`. (2) Markers placed for all itinerary items that have a linked place with lat/lng. (3) Marker popup shows item title and start time. (4) Map bounds auto-fit to the day's markers on render. (5) Map is touch-friendly on mobile (pinch-to-zoom enabled). (6) If no items on the day have coordinates, map section is hidden with "No mapped stops for this day." |
| B-016 | **Japan 2026 Trello import script**: As the trip owner I want to run a one-shot script that imports the Japan 2026 Trello export JSON so that the full 26-day, 319-item trip is populated without manual data entry. | P1 | M | done | 4 | B-004, B-006, B-007, B-008, B-011 | (1) Script at `app/scripts/import-trello.ts` reads `app/scripts/data/japan-2026.json`. (2) Accepts `--user-email` arg; resolves to Supabase `auth.users.id`. Accepts optional `--dry-run` flag: when set, the script logs all planned operations (rows to be created/upserted) and exits without writing to the database. (3) Creates one `trips` row: name "Japan 2026", start 2026-11-13, end 2026-12-08, base_currency CHF. All imported cost values are treated as CHF; no currency conversion is performed. (4) Maps Trello day lists named in format `DD.MM.YYYY` (26 open lists, 2026-11-13 to 2026-12-08) → `trip_days` rows with correctly parsed dates. Lists named with 2024 dates (closed archive lists) and all non-dated lists (`Links`, `To-Do`, `Itinerary`, `Places to Go…`, `To-Do`, `Done`) are skipped — not an error. (5) Cards in dated lists are mapped by Trello label: `Transportation` → `itinerary_items` type=transport + `transportation` row (mode inferred from card name where possible; defaults to `other`); `Hotels` → `accommodations` (hotel name extracted from card name; check-in/check-out parsed from `Checkin`/`Checkout` prefix in card name — single-card-per-stay model; if no checkout card found, check-out date defaults to next-day); `Restaurants` → `bookmarks` category=restaurant; `Attractions` → `bookmarks` category=sight; `Museums` → `bookmarks` category=museum; `Shopping` → `bookmarks` category=shopping; `Other` and unlabeled cards → `itinerary_items` type=note. Cards in non-dated lists are skipped. (6) **Place resolution for bookmarks and accommodations**: The script does NOT call the Google Places API. Place name is stored as-is in `bookmarks.notes` (free-text) and the `bookmarks.place_id` column is left null. Accommodations store the extracted hotel name in `accommodations.hotel_name`. This is the v1 import behavior; Google Places enrichment can be done separately post-import. No `google_place_id` is required at import time. (7) **Idempotency**: Script is idempotent using a `source_card_id` natural key. A `source_card_id` column (type `text`, nullable) is added to `itinerary_items`, `accommodations`, and `bookmarks` tables to store the Trello card ID. Re-running performs upserts keyed on `(trip_id, source='trello', source_card_id)` — no duplicate rows created. This is a schema change flagged for [solution-architect] R2. (8) **Error handling**: Per-card errors (malformed name, missing fields, unexpected structure) are caught individually — the card is skipped and logged to stderr with card ID and reason; the import continues. The script does NOT abort on a single bad card. Fatal errors (cannot connect to Supabase, file not found, user not found) abort immediately with a clear message and non-zero exit code. (9) On completion, logs to stdout: trips created/skipped, days created/skipped, itinerary items created/skipped, accommodations created/skipped, bookmarks created/skipped, error/skipped count. (10) Script runs via `npx ts-node app/scripts/import-trello.ts --user-email <email> [--dry-run]` against the Supabase instance defined in `.env.local` (`SUPABASE_SERVICE_ROLE_KEY` required — bypasses RLS for bulk insert). |
| B-017 | **Profile management**: As a registered user I want to set my display name and avatar so that trip partners see my name rather than my email in member lists. | P2 | S | done | 4 | B-001 | (1) Profile page at `/settings/profile` shows current display name (`profiles.full_name`) and avatar. (2) Display name (`full_name`) editable (max 80 chars); display names are NOT unique — two users may share the same name; email is the differentiator. (3) Avatar upload: accepts JPG/PNG/WebP ≤ 2MB; client-side MIME check is advisory — server must re-validate MIME type server-side and reject non-JPG/PNG/WebP with 400; stored in Supabase Storage `avatars` bucket (public-read, authenticated-write, path `avatars/{user_id}/avatar.jpg`); public URL saved to `profiles.avatar_url`. (4) On new avatar upload: delete the existing storage object at `avatars/{user_id}/avatar.jpg` before/atomically with the new upload so no orphaned files accumulate. (5) Display name and avatar reflected on all Member tab displays across trips. (6) Delete avatar: deletes storage object AND sets `profiles.avatar_url` to null; UI renders a client-side initials avatar (React component — first letter(s) of `full_name`, or first letter of email if `full_name` is null) with no server work required. (7) Storage bucket policy: `avatars` bucket is public-read; RLS restricts authenticated writes and deletes to the caller's own path prefix (`avatars/{auth.uid()}/**`). |
| B-018 | **Session protection and route guards**: As the system I need all authenticated routes to reject unauthenticated requests so that trip data is never accessible without a valid session. | P0 | S | done | 1 | B-001 | (1) All routes under `/trips`, `/invite`, `/settings`, and `/api/**` (except `/api/auth/**`) require a valid Supabase session. (2) Unauthenticated requests to protected pages redirect to `/sign-in?next=[url]`. (3) Unauthenticated requests to protected API routes return HTTP 401 with `{ error: "Unauthorized" }`. (4) Session validated server-side on every request (not from a client-readable cookie). (5) After sign-in, user is redirected to the original `next` URL. |
| B-019 | **Invitation-only access — remove public sign-up**: As the platform owner I want account creation to require a valid invitation token so that the platform is closed to outsiders and only people I invite can register. | P0 | M | done | 2 | B-012 | (1) Public `/sign-up` page removed; any direct visit redirects to `/sign-in` with a notice "Account creation is by invitation only." (2) `POST /api/auth/signup` rejects any request without a valid `invite_token` query/body param — returns 403 with `{ error: { code: "invite_required" } }`. (3) Sign-up only reachable via `/invite/[token]` landing page (from B-012); the page validates the token (exists, not expired, not used) server-side before rendering the form. (4) The sign-up form on `/invite/[token]` pre-fills the invited email (read-only) and passes the token alongside credentials to the API. (5) API re-validates the token atomically with the account creation: invalid/expired/used token → 403, no user created. (6) On success: user created, `trip_invitations.accepted_by_user_id` + `accepted_at` set, `trip_members` row inserted with the invitation's role, invitation marked used (one-shot). (7) Anti-enumeration response shape preserved (200 `{ok:true}` on success; 403 `{error}` on invite failure — distinct from email-already-registered uniform behaviour). (8) Rate limit: 10 sign-up attempts per IP per 15 minutes (tighter than the old 5/IP public limit — any attempt without valid token counts). (9) Existing user accounts (created during Sprint 1 while sign-up was open) continue to work unchanged. (10) Internal documentation note added to SOLUTION_DESIGN.md describing the bootstrap admin (first account) and the invitation-only policy. |

---

## Phase B — Enhancements

| ID | User Story | Priority | Effort | Status | Sprint | Dependencies | Notes |
|----|-----------|----------|--------|--------|--------|--------------|-------|
| B-101 | **OAuth sign-in (Google)**: As a user I want to sign in with my Google account so that I do not need a separate password for TravelPlanner. | P2 | M | backlog | TBD | B-001 | Supabase OAuth provider. Merge with existing email account if same email. |
| B-102 | **Invitation email delivery**: As a trip owner I want the invitation link emailed directly to my guest so that I do not have to copy-paste it. | P2 | S | backlog | TBD | B-012 | Supabase transactional email or Resend integration. |
| B-103 | **ICS calendar export**: As a trip member I want to export my trip itinerary as an ICS file so that I can add it to my calendar app. | P2 | M | backlog | TBD | B-006 | Generate one ICS event per itinerary item with start/end times and location. |
| B-104 | **Flight status tracking**: As a trip organizer I want live flight status for my booked segments so that I am notified of delays without checking the airline app. | P3 | L | backlog | TBD | B-007 | Third-party flight data API (e.g., AviationStack). Webhook or polling. |
| B-105 | **Trip templates**: As a trip organizer I want to create a new trip from a saved template so that recurring trip structures (e.g., annual ski trip) do not require rebuilding from scratch. | P3 | L | backlog | TBD | B-004, B-006 | Template saves trip structure (days and item types) without dates or costs. |
| B-106 | **Offline mode**: As a trip member I want the app to show my itinerary and bookmarks when I have no internet connection so that I can reference the plan in areas with poor connectivity. | P3 | XL | backlog | TBD | B-005, B-006, B-011 | Service worker + IndexedDB cache. Read-only offline; mutations queued. |

---

## Icebox

| ID | User Story | Priority | Effort | Status | Dependencies | Notes |
|----|-----------|----------|--------|--------|--------------|-------|
