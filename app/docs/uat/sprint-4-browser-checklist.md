# Sprint 4 — Browser UAT Checklist

**Tester:** You (sole UAT tester)
**Environment:** Production (Vercel) after applying migrations 0011–0014
**Prerequisites:** Migrations applied; `infowonders1@gmail.com` account accessible

Tick each item after manual verification in the browser.

---

## B-014 — Budget & expenses (2 items)

- [ ] **B014-SKIP-1 — Budget visible on trip overview (compact)**
  Open a trip's overview page. Confirm the Budget section shows "Budget: X CHF / Spent: Y CHF / Remaining: Z CHF" (or "Not set" when no budget). Set a budget on Edit trip; confirm it appears. Overspend by adding expenses that exceed the budget and confirm the remaining value renders in red.

- [ ] **B014-SKIP-2 — Pagination on Budget tab**
  Add more than 20 expenses to a trip. Open the Budget tab. Confirm a second page of results is accessible (page 2 URL or next-page control) and the correct expenses appear on each page.

---

## B-016 — Japan Trello import (3 items)

- [ ] **B016-SKIP-1 — Dry-run log output**
  Run: `npm run import:trello -- --user-email infowonders1@gmail.com --dry-run`
  Confirm the script logs `[plan]` lines for the trip, days, and each card category, and exits without creating any DB rows (check Supabase table editor).

- [ ] **B016-SKIP-2 — Live import and summary**
  Run: `npm run import:trello -- --user-email infowonders1@gmail.com`
  Confirm the script exits 0 and logs a summary line with non-zero counts for trips, days, items, accommodations, and bookmarks.

- [ ] **B016-SKIP-3 — Idempotency (re-run)**
  Re-run the import command from B016-SKIP-2. Confirm the summary shows 0 errors, and the DB row counts for `itinerary_items`, `accommodations`, and `bookmarks` are identical to those after the first run (no duplicates).

---

## B-017 — Profile management (2 items)

- [ ] **B017-SKIP-1 — Avatar upload, replace, and delete in browser**
  1. Visit `/settings/profile`.
  2. Upload a JPG avatar ≤ 2MB. Confirm it appears immediately.
  3. Upload a PNG to replace it. Confirm old JPG is gone in Supabase Storage and the new PNG is shown.
  4. Click "Remove avatar". Confirm the storage object is deleted, `profiles.avatar_url` is null, and the initials avatar renders.
  5. Attempt uploading a file with a non-image MIME type (e.g. a `.txt` renamed to `.jpg`). Confirm the upload is rejected with an error (either client-side or Storage error).

- [ ] **B017-SKIP-2 — Avatar reflected on Members tab and Budget balances**
  Upload an avatar on `/settings/profile`. Open another trip's Members tab. Confirm your avatar displays next to your name. Open the Budget tab and confirm your avatar also appears in the balances table if applicable.

---

*Generated by [tester] — Sprint 4 R5 UAT, 2026-04-28*
